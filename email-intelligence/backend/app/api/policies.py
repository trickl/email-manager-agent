"""(Removed) Policy engine API.

The repository has been re-scoped to taxonomy-driven labeling + Gmail label sync.
Policy/rules/workflow endpoints are intentionally not supported.

This module is intentionally dependency-free and is NOT wired into the FastAPI app.
"""

REMOVED = True

