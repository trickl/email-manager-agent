# Optional: set a base URL if you are not using the Vite dev proxy.
# For example, when backend is running on :8000:
VITE_API_BASE_URL=http://localhost:8000

# Demo mode (mock data for Events/Financials)
# VITE_DEMO_MODE=true
